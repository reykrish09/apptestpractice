apiVersion: v1
kind: Service
metadata:
  name: reyankrish-service
  namespace: demo
  annotations:
    cloud.google.com/neg: '{"exposed_ports":{"80":{}}}'
spec:
  type: ClusterIP
  selector:
    app: reyankrish-app
  ports:
  - port: 80
    targetPort: 8080
    protocol: TCP
